//OOP combine related units and functions into a single unit. We call that unit as object, we refer to the variables as properties and the functions as methods. This is also called as Encapsulation.
